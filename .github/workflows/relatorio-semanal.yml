name: Relatório Semanal do Dashboard

# Dispara toda segunda-feira às 13:30 UTC = 10:30 BRT (Brasil não usa horário de verão desde 2019).
on:
  schedule:
    - cron: '30 13 * * 1'
  workflow_dispatch: {} # permite disparar manualmente pela aba Actions

jobs:
  enviar-relatorio:
    runs-on: ubuntu-latest
    steps:
      - name: Chamar endpoint do relatório semanal
        run: |
          echo "Disparando geração e envio do relatório semanal..."
          code=$(curl -sS -o /tmp/resp.json -w "%{http_code}" \
            --max-time 290 \
            -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_BASE_URL }}/api/cron/relatorio-semanal")
          echo "HTTP $code"
          cat /tmp/resp.json
          echo ""
          if [ "$code" != "200" ]; then
            echo "Falha na execução (HTTP $code)"
            exit 1
          fi
